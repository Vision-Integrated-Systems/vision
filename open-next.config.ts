// Configuration for @opennextjs/cloudflare.
// Minimal setup: no R2 incremental cache. Add `incrementalCache` later if you
// create an R2 bucket. See https://opennext.js.org/cloudflare/caching
import { defineCloudflareConfig } from "@opennextjs/cloudflare";

export default defineCloudflareConfig();
